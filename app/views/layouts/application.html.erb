<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Loco Trades" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="wrapper">
      <%= render 'layouts/subnav' %>
      <%= render 'layouts/topbar' %>
      
      <div id= "flash-notice"> 
        <% flash.each do |key, value| %>
          <div class= "alert alert-box <%= key %> radius"><%= value %></div>
        <% end %>
      </div>

      <%= yield %>
      <div class="push"></div>
    </div>
      <%= render 'layouts/footer' %>
      <%= render 'devise/shared/privacy_policy' %>
    
    <%= javascript_include_tag "application" %>
    <script type="text/javascript">
      $(document).ready(
        function(){
            $('#subm-field').change(
                function(){
                    if ($(this).val()) {
                        $('#subm').attr('disabled',false); 
                    } 
                }
                );
        });
    </script>
  </body>
</html>